# tl;dr

Install the module, then run the task.

NB. Only do this on a throw-away database!

## Run

```shell
vendor/bin/sake dev/tasks/createmockdatarunner 
# OR!
vendor/bin/sake dev/tasks/createmockdatarunner --truncate
```

## Config Options

see `_config/mock-me.yml.example`
